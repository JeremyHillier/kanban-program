<Application x:Class="KanbanApp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:KanbanApp">
    <Application.Resources>
        <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#FFF1F2F5"/>
        <SolidColorBrush x:Key="CardBackgroundBrush" Color="White"/>
        <SolidColorBrush x:Key="CardBorderBrush" Color="#FFD0D0D0"/>
        <SolidColorBrush x:Key="CardOutlineBrush" Color="#FF9E9E9E"/>
        <SolidColorBrush x:Key="PrimaryTextBrush" Color="#FF1A1A1A"/>
        <SolidColorBrush x:Key="SecondaryTextBrush" Color="#FF888888"/>
        <SolidColorBrush x:Key="PanelBackgroundBrush" Color="#FFFAFAFA"/>
        <SolidColorBrush x:Key="PanelBorderBrush" Color="#FF333333"/>
        <SolidColorBrush x:Key="ButtonBackgroundBrush" Color="#FFECECEC"/>
        <SolidColorBrush x:Key="InputBackgroundBrush" Color="White"/>

        <!-- The built-in (Fluent-theme) ComboBoxItem template paints its Highlighted/Selected states
             from its own internal resources, ignoring plain Background/Foreground setters and triggers,
             and ignoring SystemColors.Highlight overrides too. A full custom template is the only
             reliable way to theme it (needed so dropdown items stay readable in dark mode). -->
        <Style TargetType="{x:Type ComboBoxItem}">
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                        <Border x:Name="Bd" Background="{DynamicResource InputBackgroundBrush}"
                                Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ButtonBackgroundBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ButtonBackgroundBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
