<UserControl x:Class="KanbanApp.Views.BulletItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" x:Name="Root">
    <!-- Two-column layout so a wrapped second line lands under the first word of the bullet's
         text, not under the bullet glyph itself - a plain single TextBlock with "• " baked into
         Text wraps flush left, under the bullet, which reads poorly for anything longer than one line. -->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="14"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Text="•" Foreground="{DynamicResource PrimaryTextBrush}"/>
        <TextBlock Grid.Column="1" Text="{Binding Text, ElementName=Root}" TextWrapping="Wrap"
                   Foreground="{DynamicResource PrimaryTextBrush}"/>
    </Grid>
</UserControl>
