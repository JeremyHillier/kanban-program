<Window x:Class="KanbanApp.Views.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        WindowStyle="None" ResizeMode="NoResize" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" ShowInTaskbar="False" Topmost="True"
        Height="320" Width="480">
    <Border CornerRadius="12" BorderBrush="#FF5B8AC4" BorderThickness="2">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#FF16283F" Offset="0"/>
                <GradientStop Color="#FF2C5282" Offset="0.55"/>
                <GradientStop Color="#FF3E6491" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <Image Source="/Assets/app.ico" Width="72" Height="72" Margin="0,0,0,16"/>
            <TextBlock Text="KANBAN TASK BOARD" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
            <TextBlock x:Name="VersionText" FontSize="12" Foreground="#FFC0CBDA" HorizontalAlignment="Center" Margin="0,8,0,0"/>
            <TextBlock x:Name="CopyrightText" FontSize="11" Foreground="#FFC0CBDA" HorizontalAlignment="Center" Margin="0,4,0,0"/>
            <TextBlock Text="Loading..." FontSize="12" Foreground="#FFC0CBDA" HorizontalAlignment="Center" Margin="0,20,0,0"/>
        </StackPanel>
    </Border>
</Window>
