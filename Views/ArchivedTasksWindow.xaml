<Window x:Class="KanbanApp.Views.ArchivedTasksWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Archived Tasks" Height="480" Width="520"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="ArchivedList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#FFE0E0E0" BorderThickness="0,0,0,1" Padding="0,8">
                            <StackPanel>
                                <TextBlock Text="{Binding Title}" FontWeight="SemiBold"/>
                                <TextBlock FontSize="11" Foreground="#FF666666" Margin="0,2,0,0">
                                    <Run Text="{Binding ColumnName, Mode=OneWay}"/>
                                    <Run Text=" • "/>
                                    <Run Text="{Binding ProjectName, Mode=OneWay}"/>
                                    <Run Text=" • archived "/>
                                    <Run Text="{Binding ArchivedAt, Mode=OneWay}"/>
                                </TextBlock>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <TextBlock x:Name="EmptyStateText" Grid.Row="0" Text="No archived tasks yet."
                   Foreground="#FF999999" HorizontalAlignment="Center" VerticalAlignment="Center"
                   Visibility="Collapsed"/>

        <Button Grid.Row="1" Content="Close" Width="90" Padding="0,6" Margin="0,12,0,0"
                HorizontalAlignment="Right" IsCancel="True"/>
    </Grid>
</Window>
