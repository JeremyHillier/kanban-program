<Window x:Class="KanbanApp.Views.PromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        x:Name="RootWindow"
        Title="New" Width="340" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource WindowBackgroundBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="PromptLabel" Text="Name" FontWeight="SemiBold" Margin="0,0,0,6"
                   Foreground="{DynamicResource PrimaryTextBrush}"/>
        <TextBox Grid.Row="1" x:Name="ValueTextBox" Padding="6"
                 Background="{DynamicResource InputBackgroundBrush}" Foreground="{DynamicResource PrimaryTextBrush}"
                 BorderBrush="{DynamicResource CardBorderBrush}"/>

        <!-- Earlier answers, shown here in the prompt rather than as a drop-down, which would open
             over the buttons below and block them. Filled by TextBoxSuggestions (inlineHost). -->
        <StackPanel Grid.Row="2" x:Name="SuggestionsHost" Visibility="Collapsed" Margin="0,10,0,0">
            <TextBlock Text="Earlier answers - click one to use it" FontSize="11" TextWrapping="Wrap"
                       Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,2"/>
        </StackPanel>

        <Grid Grid.Row="3" Margin="0,16,0,0">
            <Button x:Name="ManageLink" Content="Manage list..." Visibility="Collapsed" HorizontalAlignment="Left" VerticalAlignment="Center"
                    Click="ManageLink_Click" IsTabStop="False" Cursor="Hand" Foreground="{DynamicResource AccentTextBrush}"
                    ToolTip="Add to, reword or prune the answers suggested here">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <TextBlock x:Name="Txt" Text="{TemplateBinding Content}" Foreground="{TemplateBinding Foreground}" Background="Transparent"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Txt" Property="TextDecorations" Value="Underline"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Width="80" Padding="0,6" IsCancel="True"
                        Background="{DynamicResource ButtonBackgroundBrush}" Foreground="{DynamicResource PrimaryTextBrush}"/>
                <Button x:Name="OkButton" Content="Add" Width="80" Padding="0,6" Margin="8,0,0,0" IsDefault="True" Click="Add_Click"
                        Background="{DynamicResource ButtonBackgroundBrush}" Foreground="{DynamicResource PrimaryTextBrush}"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
