<Window x:Class="KanbanApp.Views.DashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Dashboard" Height="900" Width="1120" MinWidth="780" MinHeight="560"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="{DynamicResource WindowBackgroundBrush}">
    <!-- The tiles and chart cards are built in code (DashboardWindow.xaml.cs, DashboardCharts.cs)
         from the numbers in Services/DashboardData.cs. -->
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock x:Name="AsOfText" DockPanel.Dock="Right" VerticalAlignment="Bottom" FontSize="11"
                       Foreground="{DynamicResource SecondaryTextBrush}"/>
            <TextBlock Text="Dashboard" FontWeight="Bold" FontSize="20" Foreground="{DynamicResource PrimaryTextBrush}"/>
        </DockPanel>

        <!-- The headline numbers, one tile each. -->
        <UniformGrid x:Name="TilesPanel" Grid.Row="1" Rows="1" Margin="0,0,0,12"/>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid x:Name="ChartsGrid">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
            </Grid>
        </ScrollViewer>

        <Button Grid.Row="3" Content="Close" Width="90" Padding="0,6" Margin="0,16,0,0"
                HorizontalAlignment="Right" IsCancel="True" IsDefault="True"
                Background="{DynamicResource ButtonBackgroundBrush}" Foreground="{DynamicResource PrimaryTextBrush}"/>
    </Grid>
</Window>
